## 当前状态

- 当前迭代: **{{ iteration }}** / 最大迭代: **{{ max_iters }}**
- 最佳 RTT: **{{ "%.2f"|format(best_rtt) }}** ms
- 连续无改善: **{{ consecutive_no_improve }}** / {{ no_improve_limit }} 轮（达到上限将停止）
- 当前奖励值: **{{ "%.2f"|format(reward) }}**

## 当前参数

| 参数 | 值 |
|------|-----|
| dst_port | {{ current_params.dst_port }} |
| duration_s | {{ current_params.duration_s }} |
| pps | {{ current_params.pps }} |
| packet_size | {{ current_params.packet_size }} |
| flow_count | {{ current_params.flow_count }} |
| iat_jitter_ms | {{ current_params.iat_jitter_ms }} |

## RTT 历史（最近 {{ history|length }} 轮）

| 迭代 | RTT (ms) | 丢包率 (%) | 奖励 |
|------|----------|------------|------|
{% for h in history %}
| {{ h.iteration }} | {{ "%.2f"|format(h.rtt) }} | {{ "%.1f"|format(h.loss) }} | {{ "%.2f"|format(h.reward) }} |
{% endfor %}

请基于以上历史数据，分析 RTT 的趋势，并给出下一轮的流量参数建议。返回 JSON 格式。
